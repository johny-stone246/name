# name

Simple application that forecasts the next value of a series using linear regression without external dependencies.

## Usage

```
python forecast.py --series 1 2 3 4 5 6 --time_steps 3
```

The script prints the last sequence of values and the predicted next value.

